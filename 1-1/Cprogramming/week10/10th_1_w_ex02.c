#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h> 
// 실습2 두정수입력받아 큰수를출력하늖함수작성
/*
void get_max1(int x, int y) {
	if (x > y) {
		printf("더 큰 수는 x = %d입니다.", x);
	}
	else if (x < y) {
		printf("더 큰 수는 y = %d입니다.", y);
	}
	else {
		printf("두 수 %d %d는 같습니다.", x, y);
	}
}

int get_max2(int x, int y) {
	return (x > y) ? x : y;
}

int main(void) {
	int x, y;
	int result = 0;
	printf("두 정수를 입력하시오: ");
	scanf("%d %d", &x, &y);
	printf("===get_max1 함수 호출===\n");
	get_max1(x, y);
	printf("\n===get_max2 함수 호출===");
	printf("\n더 큰 수는: %d", get_max2(x, y));
}*/