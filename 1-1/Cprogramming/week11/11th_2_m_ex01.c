#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h> 
#define MAX 10
//크기가 10인 정수형배열 선언 초기화후 배열원소 중 홀수마 ㄴ출력하는 프로그램작성
/*
int main(void)
{
    int x[MAX] = { 0 };
    int i = 0;

    printf("배열의 원소를 입력하세요>> ");
    for (i = 0; i < MAX; i++)
        scanf("%d", &x[i]);

    for (i = 0; i < MAX; i++)
        if (x[i] % 2 == 1) {
            printf("x[%d]=%d\n", i, x[i]);
        }

    return 0;
}*/