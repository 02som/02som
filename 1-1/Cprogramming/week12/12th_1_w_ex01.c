//정수형 배열을 매개변수로 받아서 배열 원소들의 합계와 평균을 출력하는 함수 작성하기.
#include <stdio.h>
void calc_arr(int arr[], int size);      // 함수 선언
/*int sum = 0;
double avg = 0;

int main(void)
{
    int arr[5] = { 10, 20, 30, 40, 50 };
    int size = sizeof(arr) / sizeof(arr[0]);    // x 배열의 크기

    printf("배열 = ");
    calc_arr(arr, size);       // 배열 이름과 크기를 인자로 전달한다.
    printf("합계: %d, 평균: %.2lf\n", sum, avg);
    return 0;
}

void calc_arr(int arr[], int size)   // 배열형의 매개변수에서 크기는 생략한다.
{                               // 배열의 크기는 별도의 매개변수로 받아와야 한다.  
    int  i;
    sum = 0;
    for (i = 0; i < size; i++) {  // 매개변수로 받아온 배열의 크기를 이용한다.
        printf("%d ", arr[i]);
        sum += arr[i];
    }
    avg = sum / i;
    printf("\n");
}*/