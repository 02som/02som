#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h> 
#define ST_SIZE 5

//예제 5번

/*

int main(void)
{
    int score[ST_SIZE] = { 0 };
    int i, j, sum=0;
    int size = sizeof(score) / sizeof(score[0]);

    for (i = 0; i < ST_SIZE; i++) {
        printf("점수를 입력하시오: ");
        scanf("%d", &score[i]);
        sum += score[i]
    }
    printf("입력된 점수: ");
    for (i = 0; i < ST_SIZE; i++) {
        printf("%d ", score[i]);
    }
    printf("입력된 점수의 평균은: %d\n", (double)sum / size);
    return 0;
}*/

